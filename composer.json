{
    "name": "athwari/laravel-odoo-api",
    "description": "PHP Odoo JSON-RPC connector with an attribute-based model layer, prepared for Laravel integration",
    "keywords": [
        "athwari",
        "laravel",
        "php",
        "odoo",
        "jsonrpc",
        "json-rpc",
        "erp",
        "api"
    ],
    "license": "MIT",
    "type": "library",
    "homepage": "https://github.com/athwari/laravel-odoo-api",
    "authors": [
        {
            "name": "Asem Athwari",
            "email": "a.athwari@gmail.com",
            "role": "Developer"
        }
    ],
    "require": {
        "php": "^8.1",
        "guzzlehttp/guzzle": "^7.4"
    },
    "require-dev": {
        "illuminate/support": "^10.0|^11.0|^12.0",
        "orchestra/testbench": "^8.0|^9.17|^10.11",
        "laravel/pint": "^1.5 <1.21",
        "pestphp/pest": "^1.0|^2.0|^3.0",
        "pestphp/pest-plugin-laravel": "^1.0|^2.0|^3.0",
        "phpstan/extension-installer": "^1.4",
        "phpstan/phpstan": "^1.10|^2.0",
        "phpunit/phpunit": "^9.0|^10.0 <10.5.60|^11.0",
        "rector/rector": "^1.0|^2.0"
    },
    "autoload": {
        "psr-4": {
            "Athwari\\LaravelOdooApi\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Athwari\\LaravelOdooApi\\Tests\\": "tests/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Athwari\\LaravelOdooApi\\OdooApiServiceProvider"
            ],
            "aliases": {
                "OdooApi": "Athwari\\LaravelOdooApi\\Facades\\OdooApi"
            }
        },
        "branch-alias": {
            "dev-main": "2.x-dev"
        }
    },
    "scripts": {
        "test": "vendor/bin/pest",
        "test-coverage": "if php -m | grep -qi '^xdebug$'; then php -d xdebug.mode=coverage,develop vendor/bin/pest --coverage; elif php -m | grep -qi '^pcov$'; then php -d pcov.enabled=1 vendor/bin/pest --coverage; else echo 'No coverage driver detected (xdebug/pcov). Running tests without coverage.'; vendor/bin/pest; fi",
        "post-autoload-dump": "@composer run prepare",
        "prepare": "@php vendor/bin/testbench package:discover --ansi",
        "format": "vendor/bin/pint",
        "analyse": "php -d memory_limit=512M vendor/bin/phpstan analyse",
        "refactor": "vendor/bin/rector"
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "config": {
        "sort-packages": true,
        "preferred-install": "dist",
        "policy": {
            "advisories": {
                "block": false
            }
        },
        "allow-plugins": {
            "pestphp/pest-plugin": true,
            "phpstan/extension-installer": true
        }
    }
}